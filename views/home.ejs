<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ToDo - Productivity Hack App</title>
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">
    <link href="css/style.css" rel="stylesheet" />
  </head>
  <body>

    <div class="container">
      <div class="jumbotron items">
        <h2 class="text-center">I want to...</h2>

          <form action="/todo/add/" method="post">
            <div class="input-group">
              <input type="text" name="newtodo" id="newtodo" class="form-control" placeholder="add a new task" autofocus>
              <div class="input-group-btn">
                <button class="btn btn-default" type="submit">
                  <i class="glyphicon glyphicon-plus-sign"></i>
                </button>
              </div>
            </div>
          </form>
          <br />
          <!-- tasks.forEach(function(todo, index){ -->
          <ul class="list-group">
            <%  for(var index = 0;index < tasks.length; index ++){
                    if(index < completed.length && completed[index] !== 0){
            %>
            <li class="list-group-item completed">
            <% } else { %>
            <li class="list-group-item">
            <% } %>
              <a href="/todo/delete/<%= index %>"><i class="glyphicon glyphicon-trash delete"></i></a> <%= tasks[index] %>
              <a href="/todo/okay/<%= index %>"><span class="glyphicon glyphicon-ok-sign okay controls"></span></a>
              <!--<a href="#"><span class="glyphicon glyphicon-pencil edit controls"></span></a>-->
            </li>
            <span id="idx" class="theid"> <%= index %> </span>
            <% } %>
          </ul>

          <br />

          <!--
          <div class="update">
            <form action="/todo/update/" method="post">
              <div class="input-group">
                <input type="text" name="updatetodo" id="updatetodo" class="form-control" placeholder="click on the pencil button to start editing">
                <div class="input-group-btn">
                  <button class="btn btn-default" type="submit">
                    <i class="glyphicon glyphicon-thumbs-up"></i>
                  </button>
                </div>
              </div>
              <input type="text" name="idx" id="idx" class="idxpass" value="-1">
            </form>
          </div>
        -->
         </div>
       </div>

    </div>


  <!--<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.2.1/jquery.min.js"></script>-->

  </body>
</html>
