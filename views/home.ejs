<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>ToDo - Productivity Hack App</title>
  </head>
  <body>
    <h3>Your To Do List</h3>
      <ul>
        <% tasks.forEach(function(todo, index){ %>
          <li>
            <a href="/todo/delete/<%= index %>"> ✘</a> <%= todo %>
          </li>
        <% }); %>
      </ul>

      <form action="/todo/add/" method="post">
        <p>
          <label for="newtodo">Add a new task :</label>
          <input type="text" name="newtodo" id="newtodo" class="newtodo" autofocus/>
          <input type="submit" value="A D D" />
        </p>
      </form>

  </body>
</html>
